{
  "id": "dbg-py-004",
  "kind": "fix",
  "axis": "debugging",
  "language": "python",
  "tier": 3,
  "title": "RLE decoder chokes on big counts",
  "prompt": "rle_decode(encoded) expands run-length encoding: \"a3b2\" -> \"aaabb\".\nCounts can be ANY positive integer (\"x12\" -> 12 x's), and that's where it breaks.\nFind and fix the bug.",
  "functionName": "rle_decode",
  "starterCode": "def rle_decode(encoded):\n    result = []\n    i = 0\n    while i < len(encoded):\n        char = encoded[i]\n        count = int(encoded[i + 1])\n        result.append(char * count)\n        i += 2\n    return \"\".join(result)\n",
  "softTimeLimitSeconds": 600,
  "tests": [
    { "args": ["a3b2"], "expected": "aaabb" },
    { "args": ["x12"], "expected": "xxxxxxxxxxxx" },
    { "args": ["a1"], "expected": "a" },
    { "args": [""], "expected": "" },
    { "args": ["z10y2"], "expected": "zzzzzzzzzzyy" }
  ]
}
