{
  "id": "dbg-py-003",
  "kind": "fix",
  "axis": "debugging",
  "language": "python",
  "tier": 2,
  "title": "Touching intervals won't merge",
  "prompt": "merge_intervals(intervals) should merge overlapping OR touching [start, end] intervals\nand return them sorted. [1,3] and [3,5] touch, so they must merge into [1,5] —\nbut they don't. Find and fix the bug.",
  "functionName": "merge_intervals",
  "starterCode": "def merge_intervals(intervals):\n    if not intervals:\n        return []\n    intervals = sorted(intervals)\n    merged = [intervals[0][:]]\n    for start, end in intervals[1:]:\n        last = merged[-1]\n        if start < last[1]:\n            last[1] = max(last[1], end)\n        else:\n            merged.append([start, end])\n    return merged\n",
  "softTimeLimitSeconds": 420,
  "tests": [
    { "args": [[[1, 3], [3, 5]]], "expected": [[1, 5]] },
    { "args": [[[1, 4], [2, 3]]], "expected": [[1, 4]] },
    { "args": [[[1, 2], [4, 5]]], "expected": [[1, 2], [4, 5]] },
    { "args": [[[5, 7], [1, 3], [2, 6]]], "expected": [[1, 7]] },
    { "args": [[]], "expected": [] }
  ]
}
