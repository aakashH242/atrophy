{
  "id": "dbg-py-001",
  "kind": "fix",
  "axis": "debugging",
  "language": "python",
  "tier": 1,
  "title": "Word counter counts wrong",
  "prompt": "count_words(text) should return a dict mapping each lowercased word (punctuation .,!?;: stripped)\nto how many times it appears. Users report every count comes back as 1.\nFind and fix the bug — smallest change wins, don't rewrite from scratch.",
  "functionName": "count_words",
  "starterCode": "def count_words(text):\n    counts = {}\n    for word in text.lower().split():\n        word = word.strip(\".,!?;:\")\n        if not word:\n            continue\n        counts[word] = 1\n    return counts\n",
  "softTimeLimitSeconds": 300,
  "tests": [
    { "args": ["the cat and the dog"], "expected": { "the": 2, "cat": 1, "and": 1, "dog": 1 } },
    { "args": ["Stop. stop STOP!"], "expected": { "stop": 3 } },
    { "args": [""], "expected": {} },
    { "args": ["a b a"], "expected": { "a": 2, "b": 1 } }
  ]
}
